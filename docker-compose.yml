version: '3.7'
services:

  node:
    image: node:current-slim
    user: "node"
    working_dir: /home/node/
    ports:
      - "127.0.0.1:${HTTP_PORT:-80}:3000"
    volumes:
      - .:/home/node/
    command: >
      bash -c "npm install
      && npm run dev -- -H 0.0.0.0"
